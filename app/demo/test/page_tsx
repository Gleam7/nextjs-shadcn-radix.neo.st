import React from 'react';

import type { Metadata } from 'next';

import { PageHeader } from '@/components/custom-ui';
import { Grid } from './grid_tsx';

export const metadata: Metadata = {
	title: 'Table',
};

const Page = async () => {
	return (
		<>
			<PageHeader>{metadata.title?.toString()}</PageHeader>
			<Grid />
		</>
	);
};

export default Page;
